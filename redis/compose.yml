services:
  redis:
    image: 'bitnami/redis:latest'
    container_name: redis
    restart: always
    environment:
      - ALLOW_EMPTY_PASSWORD=no
      - REDIS_PASSWORD=password
    ports:
      - "6379:6379"
    networks:
      - redis

networks:
  redis:
    driver: bridge